@model IEnumerable<Kish_AndreiCezar_Project.Models.Manufacturer>
@{
    ViewData["Title"] = "Producatori";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Producatori auto</h2>
    <a class="btn btn-primary" asp-action="Create">Adauga producator</a>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Nume</th>
            <th>Tara</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Country</td>
                <td class="text-end">
                    <a class="btn btn-sm btn-outline-secondary" asp-action="Details" asp-route-id="@item.Id">Detalii</a>
                    <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@item.Id">Editeaza</a>
                    <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@item.Id">Sterge</a>
                </td>
            </tr>
        }
    </tbody>
</table>

